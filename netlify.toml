[build]
  command   = "npx expo export --platform web"
  publish   = "dist"

[build.environment]
  NETLIFY      = "true"
  NODE_VERSION = "20"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
